// Classic recursion, immutable bindings, and an if-expression.
fn fib(n: i64) -> i64 {
    if n < 2 { n } else { fib(n - 1) + fib(n - 2) }
}

fn main() {
    for i in 0..10 {
        io.println(f"fib({i}) = {fib(i)}")
    }
}
